## 项目专用术语

- **控制台**：`bro submit` 在终端显示的多窗口 TUI 页面。
- **任务树**：控制台左上部的树状结构。
- **日志窗口**：控制台右上部的日志显示区域。
- **简讯窗口**（Brief Info）：控制台中下部的 Console 区域，显示 broker 消息。
- **进度条**：控制台底部的一组进度条。
- **当前状态**：控制台最底部的单行文字。
- **员工ID**：`workers/` 下的 json 文件的 `worker`.`id` 字段的值。
- **任务名称**：`workspace/works/` 下的第一级文件夹。

详见 `docs/TERMINOLOGY.md`。
