# Python 开发规范

面向本仓库及关联项目的通用约定；具体项目可在其 `README` 或 `CONTRIBUTING` 中补充细则。

## 环境与风格

- 使用 Python 3.10+ 时，类型标注优先使用内置泛型（如 `list[str]`）与 `X | Y` 联合类型。
- 遵循 **PEP 8**；格式化与静态检查以项目配置的 **ruff** / **black** / **mypy** 为准（若有）。
- 模块级常量使用 `UPPER_SNAKE_CASE`；私有实现细节使用单下划线前缀（`_func`、`_CONST`）。
- 通用编码与设计原则（默认值与回退、单一职责、分层、工程原则，以及命名/格式/集合/并发/测试/错误与日志/SQL/分层/依赖/安全/文档目录等）见 `cursorrules/common` 的 **Design guidelines** 与 **与用户沟通**；与 `common` 合并后该段位于本文件之前。

## 错误与日志

- 可恢复的业务条件用项目约定的异常类型；不可恢复或配置错误用 `RuntimeError` / `ValueError` 等并在消息中说明如何修复。
- 日志使用模块级 `logger = logging.getLogger(__name__)`；不在库代码中 `print`。

## 测试

- 新逻辑配套单元测试；对外部 HTTP/SDK 使用 `unittest.mock.patch` 隔离。
- 测试命名：`test_<场景>_<预期>`，断言信息可读。

## 依赖与导入

- 标准库 → 第三方 → 本地应用；组间空一行；避免循环导入，必要时延迟导入或拆模块。
